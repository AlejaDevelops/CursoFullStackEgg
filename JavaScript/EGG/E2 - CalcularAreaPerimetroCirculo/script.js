alert("El numero pi es: " + Math.PI);

let radio = prompt("Ingresa el valor del radio: ");

let area = Math.PI * Math.pow(radio, 2);
let perimetro = 2 * Math.PI * radio;

alert(`El área del circulo es: ${area} y el perimetro es ${perimetro}`);
